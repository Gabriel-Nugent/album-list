const Toolbar = () => {
    return ( 
        <div className="toolbar">
            <button>Save Layout</button>
            <button id="help">?</button>
        </div>
    );
}

export default Toolbar;
